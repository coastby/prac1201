spring:
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root

server:
  servlet:
    encoding:
      force-response: true

jwt:
  token:
    secret: password